<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="12dp" /> <!-- Adjust the radius to control corner roundness -->
            <solid android:color="@color/black" /> <!-- Transparent background -->
            <stroke android:color="@android:color/transparent" android:width="8dp" /> <!-- Border color and width -->
        </shape>
    </item>
</selector>
